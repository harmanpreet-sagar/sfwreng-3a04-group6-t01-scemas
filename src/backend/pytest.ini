[pytest]
# Add src/backend to sys.path so `import app` resolves from any working directory.
# Without this, pytest running from the repo root (e.g. in CI) cannot find the
# `app` package that lives at src/backend/app/.
pythonpath = .
testpaths = tests
asyncio_mode = strict
